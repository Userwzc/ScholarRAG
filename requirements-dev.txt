# =============================================================================
# ScholarRAG 开发依赖文件
# 安装命令: pip install -r requirements-dev.txt
# =============================================================================

# 包含所有运行时依赖
-r requirements.txt

# =============================================================================
# 代码质量工具
# =============================================================================
ruff>=0.1.0,<0.16.0
bandit[toml]>=1.7.0,<2.0.0

# =============================================================================
# 测试框架
# =============================================================================
pytest>=7.4.0,<8.0.0
pytest-asyncio>=0.21.0,<0.24.0
pytest-cov>=4.1.0,<5.0.0

# =============================================================================
# 依赖对齐说明
# =============================================================================
# 已解决冲突 (2026-03-28):
# ✓ langchain 主包已卸载（原 1.2.13 版本与 langchain-core 0.2.43 不兼容）
#   解决方案: pip uninstall langchain -y
#   注意: 代码中不直接使用 langchain 主包，仅使用其子包（langchain-core, langchain-openai 等）
#
# 其他冲突:
# - numpy==1.26.4 与 cupy-cuda12x、opencv-python-headless 的 numpy>=2.0 要求冲突
#   解决方案: 这些包不在 requirements.txt 中，如需 numpy 2.x 请手动升级
# =============================================================================
